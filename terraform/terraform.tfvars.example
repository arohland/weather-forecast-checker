# Copy to terraform.tfvars (gitignored) and fill in.

project_id        = "your-project-id"
region            = "europe-west3"
bigquery_location = "europe-west3"
github_repository = "arohland/weather-forecast-checker"
state_bucket      = "your-project-id-tfstate"

# Leave unset for the first apply; set after the first image has been pushed.
# job_image = "europe-west3-docker.pkg.dev/your-project-id/wxpipe/wxpipe:<git-sha>"
