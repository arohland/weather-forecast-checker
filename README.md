# weather-forecast-checker

Extract-and-load backend (R package `wxpipe`) that ingests daily weather
observations and forecasts for Salzburg into BigQuery, run as a scheduled
Cloud Run Job.

> Work in progress -- full documentation (architecture, local setup,
> backfills, costs, data source attribution and licences) lands in the final
> documentation pass.
