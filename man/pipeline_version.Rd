% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version.R
\name{pipeline_version}
\alias{pipeline_version}
\title{Pipeline version stamped onto every ingested row}
\usage{
pipeline_version(git_sha = Sys.getenv("WXPIPE_GIT_SHA"))
}
\arguments{
\item{git_sha}{Git commit SHA. Defaults to the \code{WXPIPE_GIT_SHA}
environment variable, which the Docker build bakes into the image.
An empty string means "unknown" (e.g. a local, uncommitted run).}
}
\value{
A length-one character vector such as \code{"0.1.0+0123abc"}, or just
the package version when no SHA is available.
}
\description{
Every row written by the pipeline carries a \verb{_pipeline_version} column so
that a data problem can be traced back to the exact code that produced it.
The package version alone is not precise enough during development (many
commits share \verb{0.0.0.9000}), so the git commit the container image was
built from is appended as semver build metadata.
}
\examples{
pipeline_version(git_sha = "")
pipeline_version(git_sha = "0123abcdef")
}
