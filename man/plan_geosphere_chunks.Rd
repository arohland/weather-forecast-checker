% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client_geosphere.R
\name{plan_geosphere_chunks}
\alias{plan_geosphere_chunks}
\title{Split a date range into requests that respect the GeoSphere size limit}
\usage{
plan_geosphere_chunks(
  start_date,
  end_date,
  n_series,
  n_stations = 1L,
  max_values
)
}
\arguments{
\item{start_date, end_date}{Single \code{Date}s, \code{start_date <= end_date}.}

\item{n_series}{Number of requested series, counting \verb{_flag} companions.}

\item{n_stations}{Number of stations per request.}

\item{max_values}{The API's per-request limit (inclusive).}
}
\value{
A data frame with \code{Date} columns \code{start_date} and \code{end_date}, one
row per request.
}
\description{
Pure function. Returns contiguous, non-overlapping, inclusive date ranges
that together cover \code{start_date}..\code{end_date} exactly, each no larger than
\code{max_values}. A daily run is a single chunk; a multi-decade backfill with
many parameters is split.
}
\examples{
plan_geosphere_chunks(
  as.Date("1939-01-01"), as.Date("2026-09-13"),
  n_series = 20, max_values = 1e6
)
}
