% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_geosphere.R
\name{parse_geosphere_station}
\alias{parse_geosphere_station}
\title{Parse a GeoSphere station response into tidy observation rows}
\usage{
parse_geosphere_station(body, parameters)
}
\arguments{
\item{body}{The response body as a single JSON string.}

\item{parameters}{Character vector of the value parameters that were
requested, without their \verb{_flag} companions.}
}
\value{
A data frame with columns \code{station_id} (integer), \code{parameter}
(character), \code{reference_time_utc} (POSIXct, UTC), \code{value} (double),
\code{quality_flag} (integer) and \code{unit} (character).
}
\description{
Pure function: JSON text in, data frame out, no I/O. It turns the GeoJSON
\code{FeatureCollection} returned by
\code{GET /v1/station/historical/{resource_id}} into one row per station,
parameter and timestamp (long format). Long format means adding a
parameter to the config never changes the table schema.
}
\details{
Values are kept exactly as the API sends them. In particular, the
documented sentinels (e.g. \code{rr = -1} for "no precipitation", \code{sh = -1} for
"no snow") are not decoded here: the raw layer mirrors the source, and
decoding belongs in the downstream staging models, where it is visible and
testable. JSON \code{null} becomes \code{NA}.

The parser fails loudly, never silently dropping or coercing, when:
\itemize{
\item the body is not a GeoJSON \code{FeatureCollection};
\item a requested parameter or its \verb{_flag} companion is missing, or an
unrequested one is present;
\item a data series length does not match the number of timestamps;
\item a value is not a number or \code{null}, or a quality flag is not a whole
number.
}

Note that \code{geometry.coordinates} is returned as \verb{[lat, lon]}, the reverse
of the GeoJSON standard. It is ignored here: station coordinates come from
the configuration.
}
