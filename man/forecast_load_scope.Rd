% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_bigquery.R
\name{forecast_load_scope}
\alias{forecast_load_scope}
\title{Load scope for Open-Meteo forecasts}
\usage{
forecast_load_scope(
  location_id,
  model,
  forecast_source,
  retrieved_date = NULL,
  start_date = NULL,
  end_date = NULL,
  issued_at = NULL
)
}
\arguments{
\item{location_id}{Location id.}

\item{model}{Model id.}

\item{forecast_source}{One of \code{"forecast"}, \code{"previous_runs"}, \code{"single_runs"}.}

\item{retrieved_date}{For \code{"forecast"}: a single \code{Date}.}

\item{start_date, end_date}{For \code{"previous_runs"}: single \code{Date}s.}

\item{issued_at}{For \code{"single_runs"}: a \code{POSIXct} vector.}
}
\value{
A \code{wxpipe_load_scope} object.
}
\description{
Each forecast source owns a different slice, so that reloads replace only
the same statements and the table stays append-only:
\itemize{
\item \code{"forecast"}: the snapshot retrieved on one UTC date (\code{retrieved_date});
\item \code{"previous_runs"}: archived values valid on \code{start_date}..\code{end_date}
(UTC days), which do not change once archived;
\item \code{"single_runs"}: the runs initialised at \code{issued_at}.
}
}
