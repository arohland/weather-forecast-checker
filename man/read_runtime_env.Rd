% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env.R
\name{read_runtime_env}
\alias{read_runtime_env}
\title{Read the Google Cloud settings from environment variables}
\usage{
read_runtime_env(dry_run = FALSE, getenv = Sys.getenv)
}
\arguments{
\item{dry_run}{Whether the run writes nothing.}

\item{getenv}{Environment lookup with the signature of \code{\link[=Sys.getenv]{Sys.getenv()}};
injectable for tests.}
}
\value{
A list with \code{project}, \code{dataset} and \code{bucket} (empty strings for
unset variables in a dry run).
}
\description{
The GCP project, BigQuery dataset and GCS bucket are deliberately not in
the repository and have no defaults: the same image runs against a sandbox
or production depending only on its environment. A missing variable is an
error, except in a dry run, which writes nothing.
}
\details{
\tabular{ll}{
   Variable \tab Meaning \cr
   \code{WXPIPE_GCP_PROJECT} \tab Project that owns the dataset and is billed for queries \cr
   \code{WXPIPE_BQ_DATASET} \tab Dataset holding the raw tables (e.g. \code{raw}) \cr
   \code{WXPIPE_GCS_BUCKET} \tab Bucket for raw payload landing \cr
}
}
