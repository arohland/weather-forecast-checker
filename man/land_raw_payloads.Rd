% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcs.R
\name{land_raw_payloads}
\alias{land_raw_payloads}
\title{Land raw API responses in Cloud Storage}
\usage{
land_raw_payloads(
  records,
  source,
  run_id,
  run_date,
  bucket,
  upload = gcs_upload_file
)
}
\arguments{
\item{records}{Response records as returned by the \verb{fetch_*()} functions.}

\item{source}{Source name, e.g. \code{"geosphere"} or \code{"openmeteo"}.}

\item{run_id}{Pipeline run identifier.}

\item{run_date}{The run's start date (UTC) as a \code{Date}.}

\item{bucket}{Name of the raw landing bucket.}

\item{upload}{Function \verb{(path, bucket, object_name)} that uploads a local
file. Defaults to an upload via googleCloudStorageR; injectable for tests.}
}
\value{
A list with the object's \code{uri}, \code{object_name} and the
\code{payload_sha256} of each record.
}
\description{
Writes all response records of one run and source to a single gzipped
NDJSON file and uploads it (see the layout described in \code{R/gcs.R}).
Re-running with the same \code{run_id} overwrites the same object.
}
