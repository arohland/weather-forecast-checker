% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_openmeteo.R
\name{parse_openmeteo_hourly}
\alias{parse_openmeteo_hourly}
\title{Parse an Open-Meteo hourly response into long-format forecast rows}
\usage{
parse_openmeteo_hourly(
  body,
  variables,
  forecast_source,
  model,
  issued_at = NULL,
  lead_days = NULL
)
}
\arguments{
\item{body}{The response body as a single JSON string.}

\item{variables}{The hourly variables that were requested, as base names
(e.g. \code{"temperature_2m"}, not \code{"temperature_2m_previous_day1"}).}

\item{forecast_source}{One of \code{"forecast"}, \code{"previous_runs"} or
\code{"single_runs"}.}

\item{model}{The model id that was requested.}

\item{issued_at}{For \code{"single_runs"}, the run's initialisation time as a
single \code{POSIXct}; otherwise \code{NULL}.}

\item{lead_days}{For \code{"previous_runs"}, the requested lead-time offsets in
days; otherwise \code{NULL}.}
}
\value{
A data frame with columns \code{forecast_source}, \code{model}, \code{issued_at}
(POSIXct, UTC), \code{valid_time} (POSIXct, UTC), \code{lead_time_days} (integer),
\code{lead_time_hours} (integer), \code{variable}, \code{value} (double) and \code{unit}.
}
\description{
Pure function: JSON text in, data frame out, no I/O. Works for all three
Open-Meteo APIs used by the pipeline, which share a response shape. It
produces one row per variable, valid hour and (for Previous Runs) lead-time
offset.
}
\details{
A forecast is a statement made \emph{at} a time \emph{about} a time, and the columns
only claim what the source actually says:
\itemize{
\item \code{issued_at} is the model initialisation time. Only the Single Runs API
reports it; for the other sources it is \code{NA}, never guessed.
\item \code{lead_time_days} is the Previous Runs offset N: the value predicted
N x 24 hours before \code{valid_time}. It is \code{NA} for the other sources.
\item \code{lead_time_hours} is the exact number of hours between \code{issued_at} and
\code{valid_time}, so it is only set for Single Runs.
}

The parser requires the response to be in UTC with Unix timestamps (the
clients request \code{timezone=GMT&timeformat=unixtime}). It fails loudly on
any other time format, on missing or unrequested variables, on series that
do not line up with the timestamps, and on non-numeric values. JSON \code{null}
becomes \code{NA}; rows are never dropped.
}
