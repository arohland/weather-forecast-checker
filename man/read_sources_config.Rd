% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{read_sources_config}
\alias{read_sources_config}
\title{Read and validate the source configuration}
\usage{
read_sources_config(
  path = system.file("config", "sources.yml", package = "wxpipe")
)
}
\arguments{
\item{path}{Path to a sources YAML file. Defaults to the copy installed
with the package.}
}
\value{
The configuration as a nested list with elements
\code{config_version}, \code{locations} and \code{sources}.
}
\description{
Loads \code{inst/config/sources.yml} (or another file with the same structure)
and validates every field before anything else runs. A typo in the config
should stop the pipeline at start-up with a message naming the field, not
surface later as a confusing HTTP or BigQuery error.
}
\examples{
config <- read_sources_config()
names(config$locations)
}
