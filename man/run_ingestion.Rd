% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_ingestion}
\alias{run_ingestion}
\title{Run one planned ingestion}
\usage{
run_ingestion(
  plan,
  options,
  config,
  run_id,
  now,
  backend = NULL,
  bucket = NULL,
  upload = gcs_upload_file,
  fetchers = default_fetchers()
)
}
\arguments{
\item{plan}{Output of \code{\link[=plan_ingestion]{plan_ingestion()}}.}

\item{options}{Output of \code{\link[=parse_ingest_args]{parse_ingest_args()}}.}

\item{config}{The source configuration.}

\item{run_id}{Pipeline run identifier.}

\item{now}{Run start time; becomes \verb{_ingested_at}.}

\item{backend}{A BigQuery backend (\code{\link[=bigquery_backend]{bigquery_backend()}}); \code{NULL} in a dry run.}

\item{bucket}{Raw landing bucket; unused in a dry run.}

\item{upload}{GCS upload function (see \code{\link[=land_raw_payloads]{land_raw_payloads()}}).}

\item{fetchers}{A list of fetch functions: \code{geosphere}, \code{forecast},
\code{previous_runs} and \code{single_runs}.}
}
\value{
A data frame with one row per source: \code{source}, \code{status}
(\code{"ok"} or \code{"failed"}), \code{requests}, \code{rows} and \code{message}.
}
\description{
Run one planned ingestion
}
