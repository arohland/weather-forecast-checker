% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{validate_table}
\alias{validate_table}
\title{Validate a data frame against a table schema before loading}
\usage{
validate_table(data, schema)
}
\arguments{
\item{data}{A data frame.}

\item{schema}{A schema from \code{\link[=read_table_schema]{read_table_schema()}}.}
}
\value{
\code{data}, invisibly. Fails with a \code{wxpipe_error_schema} error
otherwise.
}
\description{
Nothing is loaded into BigQuery without passing this check. It verifies,
and never repairs:
\itemize{
\item the column set matches the schema exactly (no missing, no extra columns);
\item each column has the R type corresponding to its BigQuery type
(\code{TIMESTAMP} must be \code{POSIXct} in UTC);
\item \code{REQUIRED} columns contain no missing values;
\item values are within the column's allowed values or value catalogue, and
match its pattern, where the schema defines one;
\item no two rows share the same grain (the table's natural key).
}
}
