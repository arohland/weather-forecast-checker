% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env.R
\name{new_run_id}
\alias{new_run_id}
\title{Identifier of a pipeline run}
\usage{
new_run_id(now = Sys.time(), getenv = Sys.getenv)
}
\arguments{
\item{now}{Run start time.}

\item{getenv}{Environment lookup with the signature of \code{\link[=Sys.getenv]{Sys.getenv()}};
injectable for tests.}
}
\value{
A single string of letters, digits, dots, dashes and underscores.
}
\description{
On Cloud Run this is the job execution name (\code{CLOUD_RUN_EXECUTION}) plus
the task attempt, so log entries, raw payload files and loaded rows can be
traced to an execution in the console, and a retried task gets its own id.
Elsewhere it is a UTC timestamp with a random suffix.
}
