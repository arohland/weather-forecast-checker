% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client_geosphere.R
\name{fetch_geosphere_station}
\alias{fetch_geosphere_station}
\title{Fetch daily station data from the GeoSphere Austria Data Hub}
\usage{
fetch_geosphere_station(
  source_config,
  station_ids,
  start_date,
  end_date,
  parameters = source_config$parameters,
  max_tries = 5L,
  backoff = default_backoff
)
}
\arguments{
\item{source_config}{The \code{sources$geosphere} element of
\code{\link[=read_sources_config]{read_sources_config()}}.}

\item{station_ids}{Integer station ids.}

\item{start_date, end_date}{Single \code{Date}s.}

\item{parameters}{Value parameters to request, without \verb{_flag} suffixes.
Defaults to the configured list.}

\item{max_tries, backoff}{Retry settings; \code{backoff} is a function of the
attempt number returning seconds.}
}
\value{
A list with one element per request, each a list with \code{api}, \code{url},
\code{status}, \code{fetched_at} (UTC), \code{body_raw} (raw vector), \code{body} (string),
\code{start_date} and \code{end_date}.
}
\description{
Performs one or more \code{GET /v1/station/historical/{resource_id}} requests
covering \code{start_date}..\code{end_date} (both inclusive, as in the API). Every
parameter is requested together with its \verb{_flag} quality code. Ranges that
exceed the per-request size limit are split with \code{\link[=plan_geosphere_chunks]{plan_geosphere_chunks()}}.
}
\details{
Requests are throttled to the documented rate limits, and transient
failures (429, 5xx, timeouts) are retried with backoff. Other client errors
fail immediately with the API's explanation:
\itemize{
\item 400: range too large, or end before start;
\item 403: unknown station or parameter.
}

This function only fetches. It does not parse or store anything, so the
unmodified bodies can be landed in GCS before parsing.
}
