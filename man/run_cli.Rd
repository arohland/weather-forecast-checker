% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.R
\name{run_cli}
\alias{run_cli}
\title{Command-line entry point of the ingestion job}
\usage{
run_cli(
  args = commandArgs(trailingOnly = TRUE),
  now = Sys.time(),
  getenv = Sys.getenv,
  dependencies = list()
)
}
\arguments{
\item{args}{Command-line arguments.}

\item{now}{Run start time.}

\item{getenv}{Environment lookup with the signature of \code{\link[=Sys.getenv]{Sys.getenv()}};
injectable for tests.}

\item{dependencies}{A list overriding any of \code{fetchers}, \code{authenticate},
\code{backend} and \code{upload} (see \code{default_dependencies()}); for tests.}
}
\value{
The exit status as an integer.
}
\description{
Called by \code{exec/ingest.R}. Parses and validates the arguments, reads the
runtime environment, connects to Google Cloud (unless \code{--dry-run}), runs
the ingestion and logs every step as structured JSON.
}
\details{
Exit statuses, which Cloud Run Jobs use to mark an execution failed:
\itemize{
\item \code{0}: every requested source succeeded;
\item \code{1}: at least one source failed, or Google Cloud was unreachable (the
other sources still ran);
\item \code{2}: invalid command line or configuration; nothing was fetched.
}
}
