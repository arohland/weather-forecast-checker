% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client_openmeteo.R
\name{fetch_openmeteo_single_runs}
\alias{fetch_openmeteo_single_runs}
\title{Fetch complete model runs from the Single Runs API}
\usage{
fetch_openmeteo_single_runs(
  source_config,
  location,
  run_times,
  max_tries = 5L,
  backoff = default_backoff
)
}
\arguments{
\item{source_config}{The \code{sources$openmeteo} element of
\code{\link[=read_sources_config]{read_sources_config()}}.}

\item{location}{A location from \code{\link[=get_location]{get_location()}}.}

\item{run_times}{A \code{POSIXct} vector of model initialisation times. Each
must fall on a configured cycle hour (UTC) and not precede the configured
archive start.}

\item{max_tries, backoff}{Retry settings; \code{backoff} is a function of the
attempt number returning seconds.}
}
\value{
A list of response records, one per run, with added
\code{forecast_source} (\code{"single_runs"}), \code{model}, \code{issued_at} (the run time)
and \code{variables}.
}
\description{
Fetch complete model runs from the Single Runs API
}
