% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time.R
\name{parse_utc_timestamp}
\alias{parse_utc_timestamp}
\title{Parse ISO 8601 timestamps with an explicit UTC offset}
\usage{
parse_utc_timestamp(x)
}
\arguments{
\item{x}{Character vector of timestamps.}
}
\value{
A \code{POSIXct} vector in UTC, the same length as \code{x}.
}
\description{
All timestamps are stored as UTC. This parser therefore accepts only
timestamps that carry an explicit offset (\code{Z}, \code{+00:00}, \code{+0200}, ...):
a timestamp without one is ambiguous, and guessing its zone is exactly
the kind of silent coercion the pipeline must not do.
}
\details{
Accepted shapes are \verb{YYYY-MM-DDTHH:MM[:SS]} followed by \code{Z}, \code{+HH:MM} or
\code{+HHMM}, which covers what the GeoSphere API returns
(e.g. \code{"2026-09-10T00:00+00:00"}).
}
\examples{
parse_utc_timestamp(c("2026-09-10T00:00+00:00", "2026-09-10T02:00+02:00"))
}
