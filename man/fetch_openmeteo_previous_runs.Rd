% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client_openmeteo.R
\name{fetch_openmeteo_previous_runs}
\alias{fetch_openmeteo_previous_runs}
\title{Fetch archived forecasts at fixed lead times from the Previous Runs API}
\usage{
fetch_openmeteo_previous_runs(
  source_config,
  location,
  start_date,
  end_date,
  max_tries = 5L,
  backoff = default_backoff
)
}
\arguments{
\item{source_config}{The \code{sources$openmeteo} element of
\code{\link[=read_sources_config]{read_sources_config()}}.}

\item{location}{A location from \code{\link[=get_location]{get_location()}}.}

\item{start_date, end_date}{Single \code{Date}s. \code{start_date} must not be earlier
than the configured archive start.}

\item{max_tries, backoff}{Retry settings; \code{backoff} is a function of the
attempt number returning seconds.}
}
\value{
A list of response records, one per chunk, with added
\code{forecast_source} (\code{"previous_runs"}), \code{model}, \code{issued_at} (\code{NULL}),
\code{variables}, \code{lead_days}, \code{start_date} and \code{end_date}.
}
\description{
Requests every configured variable at every configured lead-time offset
for \code{start_date}..\code{end_date} (valid dates, UTC, inclusive), split into
chunks of \code{previous_runs.chunk_days}.
}
