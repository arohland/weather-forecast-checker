% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{authenticate_gcp}
\alias{authenticate_gcp}
\title{Authenticate to Google Cloud using gargle's default credential chain}
\usage{
authenticate_gcp(
  scopes = "https://www.googleapis.com/auth/cloud-platform",
  fetch_token = gargle::token_fetch
)
}
\arguments{
\item{scopes}{OAuth scopes; \code{cloud-platform} covers BigQuery and Cloud
Storage.}

\item{fetch_token}{Function returning a token or \code{NULL}; injectable for
tests.}
}
\value{
The token, invisibly.
}
\description{
No credentials are handled or stored by the pipeline itself.
\code{gargle::token_fetch()} tries, in order: a bring-your-own token, a service
account key, external account (workload identity federation), Application
Default Credentials, and the GCE metadata server. Locally, ADC come from
\verb{gcloud auth application-default login}. On Cloud Run the metadata server
provides a token for the job's attached service account. The same token is
passed to bigrquery and googleCloudStorageR.
}
